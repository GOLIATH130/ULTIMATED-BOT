const welcome = (number, groupname) => {
    return `Joder  @${number}.Que bueno verte por aca 🥺 ${groupname} Recuerda leer las reglas para evitar mal entendidos😉`
}
exports.welcome = welcome

const bye = (number) => {
    return `Que bueno que te fuiste🤩 @${number}. 😤Ya no te queríamos ojala nunca regreses bye 👋`
}
exports.bye = bye